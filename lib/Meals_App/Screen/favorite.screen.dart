import 'package:flutter/material.dart';

class FaveroiteScreen extends StatelessWidget {
  const FaveroiteScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
